/**
 * Host-side controls (shadcn create Customizer role).
 * Implementation lives in studio-controls; this is the create-parity export name.
 */
export {
  StudioControls as Customizer,
  type ProjectInfo,
} from "@/components/studio-controls"
