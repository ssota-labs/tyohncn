/**
 * Preview iframe document layout — scrollable, no host chrome.
 * Style packs load via root globals.css; DesignSystemProvider scopes body.
 */
export default function PreviewLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
