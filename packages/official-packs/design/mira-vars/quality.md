# mira-vars pack quality bar

- [ ] `pack.json` validates (`tyohncn pack validate mira-vars`)
- [ ] Style scoped under `.style-mira`
- [ ] Exposes `--cn-button-height` (and related density vars)
- [ ] Does not overwrite project theme unless author adds `theme`
- [ ] `apply` / `pack add` leaves component TSX mtimes unchanged
