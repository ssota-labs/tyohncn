# ssota pack quality bar

- [ ] `pack.json` validates (`tyohncn pack validate ssota`)
- [ ] Style scoped under `.style-ssota` only
- [ ] Components still contain `cn-*` after install (no inline strip)
- [ ] Theme tokens include `--primary`, `--background`, `--radius`
- [ ] App root uses `className="style-ssota"`
- [ ] Studio can read `--cn-button-height` under the ssota scope
